def smallest_number(a, b, c):

    if a < b and a < c:
        return a
    elif b < a and b < c:
        return b
    elif c < a and c < b:
        return c

a_num = int(input())
b_num = int(input())
c_num = int(input())

print(smallest_number(a_num, b_num, c_num))
